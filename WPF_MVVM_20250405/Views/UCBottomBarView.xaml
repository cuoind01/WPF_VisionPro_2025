<UserControl x:Class="WPF_MVVM_20250405.Views.UCBottomBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MVVM_20250405.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <UniformGrid Rows="1" >
        <Button Margin="2" Content="{Binding ContentAuto}" Command="{Binding AutoCommand}"/>
        <Button Margin="2" Content="{Binding ContentTeaching}" Command="{Binding TeachingCommand}"/>
        <Button Margin="2" Content="{Binding ContentSetting}" Command="{Binding SettingCommand}"/>
        <Label/>
        <Button Margin="2" Content="{Binding ContentExit}" Command="{Binding ExitCommand}"/>
    </UniformGrid>
</UserControl>
