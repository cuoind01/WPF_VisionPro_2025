<UserControl x:Class="WPF_MVVM_20250405.Views.UCTopBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPF_MVVM_20250405.Views"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="800">
    <Grid Background="Transparent">
        <UniformGrid Columns="5">
            <UniformGrid Columns="1">
                <Label Content="{Binding LbTime}"
                 VerticalContentAlignment="Center"
                 HorizontalContentAlignment="Center"
                 FontSize="15"
                 />
                <Label  Content="{Binding LbDate}"
                  
                  VerticalContentAlignment="Center"
                  HorizontalContentAlignment="Center"
                  FontSize="20"
                  FontWeight="Bold"
                  />
            </UniformGrid>
            <UniformGrid Columns="1">
                <TextBlock Text="{Binding TxtData, UpdateSourceTrigger=PropertyChanged}"
                             FontSize="15"
                             />
                <TextBox  Text="{Binding TxtData,UpdateSourceTrigger=PropertyChanged}"
  
                              VerticalContentAlignment="Center"
                              HorizontalContentAlignment="Center"
                              FontSize="20"
                              FontWeight="Bold"
                              />
            </UniformGrid>
        </UniformGrid>
    </Grid>
</UserControl>
